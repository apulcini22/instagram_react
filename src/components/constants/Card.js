import React from 'react';

const Card = ({ children }) => {
    console.log('Card objects: ', imageInfo.images.thumbnail.props)
    return (
        <div>
            { this.props.children }
        </div>
    );
}
export default Card;